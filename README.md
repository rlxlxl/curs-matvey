# Система управления логистическими перевозками

Безопасное веб-приложение для управления логистическими перевозками с защитой от SQL-инъекций, построенное на C++ бэкенде и PostgreSQL базе данных.

## 🚀 Особенности

- **Безопасность**: Защита от SQL-инъекций через использование prepared statements
- **Современный стек**: C++ бэкенд, PostgreSQL база данных, современный веб-интерфейс
- **Админ-панель**: Полнофункциональная панель администратора для управления перевозками
- **CRUD операции**: Создание, чтение, обновление и удаление записей о перевозках
- **Docker**: Полная контейнеризация приложения с Docker Compose
- **Адаптивный дизайн**: Современный и удобный пользовательский интерфейс

## 📋 Требования

- Docker (версия 20.10 или выше)
- Docker Compose (версия 1.29 или выше)

## 🛠️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/rlxlxl/curs-matvey
cd curs-matvey
```

### 2. Запуск приложения

#### Вариант 1: Использование Makefile (рекомендуется)

```bash
make up-build
```

Или используйте другие команды Makefile:
```bash
make help          # Показать все доступные команды
make build         # Собрать образы
make up           # Запустить контейнеры
make logs          # Показать логи
make db-reset      # Сбросить базу данных
```

#### Вариант 2: Использование Docker Compose напрямую

```bash
docker-compose up --build
```

Эта команда:
- Создаст и запустит контейнер PostgreSQL с предустановленной схемой базы данных
- Соберет и запустит C++ бэкенд сервер
- Приложение будет доступно по адресу `http://localhost:8080`

### 3. Остановка приложения

```bash
make down          # Остановить контейнеры
make down-volumes  # Остановить и удалить volumes
```

Или через Docker Compose:

```bash
docker-compose down
docker-compose down -v  # С удалением данных
```

## 📖 Использование

### Веб-интерфейс

Откройте браузер и перейдите по адресу `http://localhost:8080`

### Функции администратора

1. **Просмотр перевозок**: Все перевозки отображаются в таблице на главной странице
2. **Добавление перевозки**: Заполните форму в панели администратора и нажмите "Добавить перевозку"
3. **Редактирование перевозки**: Нажмите кнопку "Редактировать" рядом с нужной перевозкой
4. **Удаление перевозки**: Нажмите кнопку "Удалить" и подтвердите действие

### Поля перевозки

- **Описание груза** (обязательно): Описание перевозимого груза
- **Откуда** (обязательно): Город отправления
- **Куда** (обязательно): Город назначения
- **Вес (кг)** (обязательно): Вес груза в килограммах
- **Объем (м³)** (опционально): Объем груза в кубических метрах
- **Тип транспорта** (обязательно): Тип транспортного средства (Грузовик, Фура, Рефрижератор, Контейнер)
- **Статус** (обязательно): Текущий статус перевозки (Ожидает, В пути, Доставлено)

## 🔒 Безопасность

### Защита от SQL-инъекций

Приложение использует **prepared statements** (параметризованные запросы) для всех операций с базой данных:

- **Создание**: `PQexecParams` с параметрами `$1, $2, $3...`
- **Обновление**: `PQexecParams` с параметрами
- **Удаление**: `PQexecParams` с параметрами

Все пользовательские данные передаются как параметры, а не встраиваются напрямую в SQL-запросы, что полностью исключает возможность SQL-инъекций.

### Пример защищенного запроса:

```cpp
const char* query = "INSERT INTO shipments (cargo_description, origin, destination, weight_kg, volume_m3, transport_type, status) VALUES ($1, $2, $3, $4, $5, $6, $7)";
const char* paramValues[7] = {cargo, origin, destination, weight, volume, transport, status};
PGresult* res = PQexecParams(conn, query, 7, nullptr, paramValues, paramLengths, paramFormats, 0);
```

## 🏗️ Архитектура проекта

```
curs-matv/
├── backend/
│   ├── main.cpp          # C++ HTTP сервер и API
│   └── Dockerfile        # Docker образ для бэкенда
├── frontend/
│   └── index.html        # Веб-интерфейс
├── database/
│   └── init.sql          # SQL скрипт инициализации БД
├── docker-compose.yml    # Конфигурация Docker Compose
├── .gitignore           # Игнорируемые файлы Git
└── README.md            # Документация
```

## 🗄️ База данных

### Реляционная структура

База данных использует реляционную модель с несколькими связанными таблицами:

#### Таблица `clients` (Клиенты/Заказчики)

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Уникальный идентификатор |
| name | VARCHAR(255) | Название компании/ФИО |
| contact_person | VARCHAR(255) | Контактное лицо |
| phone | VARCHAR(50) | Телефон |
| email | VARCHAR(255) | Email |
| address | TEXT | Адрес |
| created_at | TIMESTAMP | Дата создания |

#### Таблица `vehicles` (Транспортные средства)

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Уникальный идентификатор |
| license_plate | VARCHAR(20) | Номерной знак (уникальный) |
| vehicle_type | VARCHAR(50) | Тип транспорта |
| capacity_kg | DECIMAL(10,2) | Грузоподъемность в кг |
| volume_m3 | DECIMAL(10,2) | Объем в кубических метрах |
| status | VARCHAR(50) | Статус (available, in_use) |
| created_at | TIMESTAMP | Дата создания |

#### Таблица `drivers` (Водители)

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Уникальный идентификатор |
| full_name | VARCHAR(255) | ФИО водителя |
| license_number | VARCHAR(50) | Номер водительского удостоверения (уникальный) |
| phone | VARCHAR(50) | Телефон |
| email | VARCHAR(255) | Email |
| status | VARCHAR(50) | Статус (available, in_use) |
| created_at | TIMESTAMP | Дата создания |

#### Таблица `shipments` (Перевозки)

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Уникальный идентификатор |
| client_id | INTEGER | Ссылка на клиента (FK) |
| vehicle_id | INTEGER | Ссылка на транспорт (FK) |
| driver_id | INTEGER | Ссылка на водителя (FK) |
| cargo_description | VARCHAR(255) | Описание груза |
| origin | VARCHAR(255) | Город отправления |
| destination | VARCHAR(255) | Город назначения |
| weight_kg | DECIMAL(10,2) | Вес в килограммах |
| volume_m3 | DECIMAL(10,2) | Объем в кубических метрах |
| status | VARCHAR(50) | Статус перевозки |
| departure_date | TIMESTAMP | Дата отправления |
| arrival_date | TIMESTAMP | Дата прибытия |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата последнего обновления |

### Связи между таблицами

- `shipments.client_id` → `clients.id` (ON DELETE SET NULL)
- `shipments.vehicle_id` → `vehicles.id` (ON DELETE SET NULL)
- `shipments.driver_id` → `drivers.id` (ON DELETE SET NULL)

### Представление `shipments_full`

Создано представление для удобного просмотра перевозок со всеми связанными данными:
- Информация о клиенте
- Информация о транспорте
- Информация о водителе

### API Endpoints

- `GET /api/shipments` - Получить все перевозки
- `POST /api/shipments` - Создать новую перевозку
- `PUT /api/shipments/{id}` - Обновить перевозку
- `DELETE /api/shipments/{id}` - Удалить перевозку

## 🐳 Docker контейнеры

### PostgreSQL
- **Порт**: 5432
- **База данных**: logistics_db
- **Пользователь**: logistics_user
- **Пароль**: logistics_pass

### Backend
- **Порт**: 8080
- **Язык**: C++
- **Библиотека**: libpq (PostgreSQL client)

## 🔧 Разработка

### Использование Makefile

Проект включает Makefile для упрощения работы:

```bash
make help          # Показать все доступные команды
make build         # Собрать Docker образы
make up            # Запустить контейнеры
make down          # Остановить контейнеры
make logs          # Показать логи всех сервисов
make logs-backend  # Показать логи только backend
make logs-postgres # Показать логи только PostgreSQL
make ps            # Показать статус контейнеров
make rebuild       # Пересобрать и перезапустить
make db-connect    # Подключиться к PostgreSQL через psql
make test          # Запустить тесты API
make clean         # Очистить контейнеры и сети
make clean-all     # Полная очистка (включая volumes)
make db-reset      # Сбросить базу данных
```

### Локальная разработка (без Docker)

Если вы хотите запустить приложение локально:

1. Установите PostgreSQL и создайте базу данных
2. Выполните `database/init.sql` для создания схемы
3. Скомпилируйте бэкенд:
   ```bash
   cd backend
   g++ -std=c++17 -I/usr/include/postgresql -o server main.cpp -lpq -pthread
   ```
4. Запустите сервер:
   ```bash
   ./server
   ```

### Изменение конфигурации базы данных

Отредактируйте переменные окружения в `docker-compose.yml`:

```yaml
environment:
  DB_HOST: postgres
  DB_PORT: 5432
  DB_NAME: logistics_db
  DB_USER: logistics_user
  DB_PASSWORD: logistics_pass
```

## 📝 Технологии

- **Backend**: C++17, libpq (PostgreSQL client library)
- **Database**: PostgreSQL 15
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla)
- **Containerization**: Docker, Docker Compose
- **Web Server**: Custom HTTP server на C++

## 🐛 Решение проблем

### Проблема: Контейнер не запускается

**Решение**: Убедитесь, что порты 8080 и 5432 свободны:
```bash
lsof -i :8080
lsof -i :5432
```

### Проблема: Ошибка подключения к базе данных

**Решение**: Убедитесь, что PostgreSQL контейнер запущен и здоров:
```bash
docker-compose ps
docker-compose logs postgres
```

### Проблема: Файлы не найдены

**Решение**: Убедитесь, что все файлы на месте и структура проекта правильная.

## 📄 Лицензия

Этот проект создан в учебных целях.


